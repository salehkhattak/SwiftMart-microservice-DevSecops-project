apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: swiftmart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: swiftmart-notification-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 5005
          envFrom:
            - configMapRef:
                name: notification-config
            - secretRef:
                name: notification-secret
          resources:
              requests:
                  cpu: "100m"
                  memory: "128Mi"
              limits:
                  cpu: "500m"
                  memory: "512Mi"
          readinessProbe:
              httpGet:
                  path: /health
                  port: 5005
              initialDelaySeconds: 10
              periodSeconds: 5

          livenessProbe:
              httpGet:
                 path: /health
                 port: 5005
              initialDelaySeconds: 20
              periodSeconds: 10